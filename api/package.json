{
  "name": "portfolio-generator-api",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "node_modules/.bin/json-server db.json --port ${PORT:-4000}",
    "dev": "node_modules/.bin/json-server --watch db.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
